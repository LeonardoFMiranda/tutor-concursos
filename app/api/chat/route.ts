import { NextResponse } from "next/server";

// Placeholder — será implementado na Fase 5
export async function POST() {
  return NextResponse.json(
    { error: "Tutor ainda não implementado" },
    { status: 501 }
  );
}
